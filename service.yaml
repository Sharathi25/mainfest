apiVersion: v1
kind: Service
metadata:
  name: hello-java-service
  namespace: java
spec:
  selector:
    app: hello-java
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: NodePort
